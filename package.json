{
  "name": "cf-workers-books-api",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev --test-scheduled --live-reload=false",
    "deploy": "wrangler deploy",
    "test": "vitest run",
    "test:watch": "vitest",
    "format": "prettier -w ."
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240925.0",
    "miniflare": "^3.20240925.0",
    "prettier": "^3.3.3",
    "typescript": "^5.6.3",
    "vitest": "^2.1.3",
    "wrangler": "^4.45.0"
  }
}